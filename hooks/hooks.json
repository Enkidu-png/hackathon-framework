{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"try{require('child_process').execSync('git pull --rebase --quiet',{stdio:'ignore',timeout:10000})}catch(e){}\"",
            "timeout": 12
          }
        ],
        "description": "Git pull on session start"
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"try{const r=require('child_process').execSync('git status --porcelain',{encoding:'utf8',timeout:3000});if(r.trim())require('child_process').execSync('git add -A && git commit -m \\\"savepoint: auto-save\\\" --no-verify',{stdio:'ignore',timeout:5000})}catch(e){}\"",
            "timeout": 8
          }
        ],
        "description": "Savepoint on turn end"
      }
    ]
  }
}
